
const DefButton = (props) => {
  return (
    <div>
      <button className="btn btn-danger" style={{transition:".5s"}}>
         {props.text}
         </button>

    </div>
  )
}

export default DefButton
